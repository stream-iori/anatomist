# Maven non-flow edge cases

Persistent Maven-only fixture for selector ambiguity, overload families, Spring
route expansion, constructor injection, XML bean pagination, and generated-source
scope tests. Tests copy `generated-template` into a temporary module `target` tree.
