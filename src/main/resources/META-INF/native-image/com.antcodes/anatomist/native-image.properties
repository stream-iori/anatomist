# anatomist hand-written native-image hints. picocli-codegen and sqlite-jdbc
# both ship their own META-INF/native-image entries — those are picked up
# automatically and do not need to be re-declared here. This file only adds
# what we own: the schema.sql resource and (future) reflect-config residuals
# captured by the GraalVM tracing-agent on IndexCommandIT / CommonsLangSmokeIT.
