# PRD: [TASK NAME]

**Source**: [proposal.md](./proposal.md)
**Tasks**: [tasks.md](./tasks.md)

<!--
  由 AI Agent 在 specify 步骤生成。
  基于 proposal.md 中的用户意图，通过五维挑战（完整性/一致性/可行性/可测性/影响面）
  和代码锚定，产出可落地的产品需求文档。

  最低完成要求:
    - 至少包含一个 REQ-编号的功能需求，或
    - 至少包含一个场景规格（### S1）
-->

## 1. 需求概述

<!--
  从意图提炼的一句话摘要。回答"做什么"和"为什么"。
-->

[需求概述]

## 2. 术语表

<!--
  项目级术语来自 facts/glossary.json，本需求级术语在此补充。
  格式：术语 — 定义
-->

| 术语 | 定义 |
|------|------|
| [术语1] | [定义] |

## 3. 功能需求

<!--
  每条需求可独立验证，带唯一编号便于追溯。
-->

- **REQ-001**: [可验证的需求陈述]
- **REQ-002**: [可验证的需求陈述]

## 4. 业务规则

<!--
  业务约束，关联到功能需求。
-->

- **BR-001**: [规则] → 关联 REQ-001
- **BR-002**: [规则] → 关联 REQ-002

## 5. 场景规格

<!--
  结构化场景，Given/When/Then 格式。
  每个场景关联到 REQ 编号，便于追溯。
-->

### S1: [场景名称] [REQ-001]

- **Given** [前置条件]
- **When** [触发操作]
- **Then** [期望结果]

### S2: [场景名称] [REQ-002]

- **Given** [前置条件]
- **When** [触发操作]
- **Then** [期望结果]

[按需添加更多场景]

## 6. 变更时序图

<!--
  从人可读的角度，用时序图呈现本次变更涉及的核心链路。
  对照 domain-model.md 中已有的时序图，帮助读者快速理解"变了什么"。
-->

### 变更链路时序图

<!--
  生成本次需求涉及的变更链路的时序图（Mermaid sequenceDiagram）。
  聚焦本次变更的调用路径，标注新增/修改的步骤（用 note 标注 [NEW]/[MOD]）。
  参与者控制在 6 个以内，链路过长按边界拆分多张图。
-->

[变更时序图]

### 参考时序图（来自 domain-model）

<!--
  从 .diorama/knowledge/facts/domain-model.md 中复制与本次变更相关的时序图，
  作为对照参考，帮助读者理解变更前后的差异。
  只复制与本次变更相关的场景时序图，不需要复制全部。
-->

[参考时序图]

### 变更模型图

<!--
  用 Mermaid classDiagram 呈现本次需求涉及的模型结构变更前后的对比。
  只画变更涉及的实体及其直接关联，不画全量领域模型。

  必须同时提供"变更前"和"变更后"两张模型图，便于对比。
  变更后模型使用 Mermaid classDef 高亮样式区分变更类型：
    - 新增实体  → :::new     绿色背景 (#d4edda)
    - 修改实体  → :::modified 黄色背景 (#fff3cd)
    - 删除实体  → :::deleted  红色背景 (#f8d7da)，虚线边框

  实体内部字段级标注：
    - 新增字段 → 行尾注释 [NEW]
    - 修改字段 → 行尾注释 [MOD]，并附变更说明
    - 删除字段 → 仅出现在变更前模型，变更后模型中移除
    - 未变更字段 → 不加标注，作为上下文保留

  如果本次需求不涉及模型结构变更（纯行为/接口变更），填写"无模型变更"即可。
-->

#### 变更前模型

<!--
  画变更前的模型结构，作为对比基线。
  仅画与本次变更相关的实体及其直接关联。
-->

[变更前模型图]

#### 变更后模型

<!--
  画变更后的模型结构，使用 classDef 高亮样式突出变更实体。
  示例：

  ```mermaid
  classDiagram
    class Order {
      +String orderId [MOD: 原为 Long]
      +BigDecimal amount
      +String currency [NEW]
      +Instant createdAt
    }:::modified

    class PaymentRecord {
      +String paymentId
      +String orderId
      +String currency [NEW]
    }:::new

    class RefundLog {
      +String refundId
      +String reason
    }:::deleted

    Order "1" -- "many" PaymentRecord : pays

    classDef new fill:#d4edda,stroke:#28a745,stroke-width:2px
    classDef modified fill:#fff3cd,stroke:#ffc107,stroke-width:2px
    classDef deleted fill:#f8d7da,stroke:#dc3545,stroke-width:2px,stroke-dasharray: 5 5
  ```
-->

[变更后模型图]

## 7. 实现上下文

<!--
  从代码扫描获得，指定落地路径。
  纯业务语言描述场景，禁止技术术语泄漏。
-->

- **入口代码**: [Controller/Handler/Job/Service 类与方法]
- **SUT 边界**: [被测系统范围] + [外部依赖 → 隔离策略]
- **Stub/Mock 策略**: [哪些依赖 stub、哪些 mock、哪些用真实实现]

### 变更清单

#### 新增
- [需要新建的类/接口/文件]

#### 修改
- [需要改动的现有代码]

#### 删除
- [需要删除的内容]

## 8. 接口契约

<!--
  分析本次需求是否涉及接口层面的变更。
  如果涉及，逐条列出接口契约的变更内容，便于上下游评估影响。
  如果本次需求不涉及接口变更，填写"无接口变更"即可。
-->

### 新增接口

| 接口 | 方法/路径 | 请求体 | 响应体 | 说明 |
|------|----------|--------|--------|------|
| [接口名] | [POST /api/xxx] | [请求结构] | [响应结构] | [用途] |

### 修改接口

| 接口 | 变更类型 | 变更前 | 变更后 | 兼容性 |
|------|---------|--------|--------|--------|
| [接口名] | [新增字段/修改字段/废弃字段] | [原结构] | [新结构] | [向前兼容/破坏性变更] |

### 删除接口

| 接口 | 原方法/路径 | 替代方案 |
|------|-----------|---------|
| [接口名] | [DELETE /api/xxx] | [替代接口或无] |

## 9. 影响面与风险

- **不包含**: [明确排除的范围]
- **已知风险**: [改动可能引发的问题]
- **依赖**: [前置条件/阻塞关系]

## 10. 验收标准

<!--
  每条验收标准可客观判定，关联到 REQ 编号。
-->

- **AC-001**: [REQ-001] [可客观判定的验收条件]
- **AC-002**: [REQ-002] [可客观判定的验收条件]

### 验证矩阵

| 验证项 | 阶段 | 手段 | 本次覆盖 |
|--------|------|------|---------|
| [验证项] | [集成测试/E2E/运营] | [DSL/手工/监控] | — |

## 11. 遗留问题

<!--
  最多允许 3 个 [NEEDS CLARIFICATION] 标记。
  超过则应继续追问用户，不应提交 PRD。
-->

- [NEEDS CLARIFICATION] [未明确的问题]

<!--
  ## Amendments

  在 generate 阶段通过 design-amend 机制追加的轻量修改记录。
  此段由脚本自动生成，无需手动添加。
  格式：- **<ISO timestamp>**: <修改摘要>
-->
